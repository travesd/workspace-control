# Core Specs

This directory is reserved for portable operating-model specs.

Likely future homes:

- task resumability concepts,
- generic task lifecycle vocabulary,
- durable learning routing,
- cross-agent review and delegation rules.

Current canonical specs remain under `docs/specs/` until a render/sync path is
approved.

