# Core Templates

This directory is reserved for portable templates such as resume packets,
notes-ledger entries, and review handoff blocks.

Current canonical templates remain under `docs/templates/` until a render/sync
path is approved.
