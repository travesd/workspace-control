# Workspace Status Prompt

Use the `workspace-status` skill and run `tools/workspace-status`.

Return only:

- current task context,
- session/recovery warnings,
- relevant active work,
- next action.

Do not mutate task state.
